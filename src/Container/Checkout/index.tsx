import { ComponentCheckOutContent, StyledWrapContent } from "@components";

export const CheckOut = () => {
    return (
        <StyledWrapContent>
            <div className="wrapperContent">
                <ComponentCheckOutContent />
            </div>
        </StyledWrapContent>
    );
};
